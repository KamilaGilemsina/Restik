<Window x:Class="Restik.Windows.AddEditDishWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restik.Windows"
        mc:Ignorable="d"
        Title="AddEditDishWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Название:" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="tbName" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Описание:" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Top"/>
        <TextBox x:Name="tbDescription" Grid.Row="1" Grid.Column="1" Margin="5" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>

        <TextBlock Text="Цена:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="tbPrice" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Категория:" Grid.Row="3" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbCategory" Grid.Row="3" Grid.Column="1" Margin="5" DisplayMemberPath="Name" SelectedValuePath="ID"/>

        <CheckBox x:Name="chkAvailable" Grid.Row="4" Grid.ColumnSpan="2" Margin="5" Content="Доступно"/>

        <Button x:Name="btnSave" Content="Сохранить" Click="btnSave_Click" Grid.Row="5" Grid.ColumnSpan="2" Width="100" HorizontalAlignment="Center" Margin="5"/>

        <TextBlock x:Name="tbMessage" Grid.Row="6" Grid.ColumnSpan="2" Foreground="Red" Margin="5" HorizontalAlignment="Center"/>
    </Grid>
</Window>
