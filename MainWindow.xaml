<Window x:Class="Restik.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restik"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Rectangle Fill="LightBlue" Opacity="0.8"/>
        <Ellipse Margin="10" Width="80" Height="80" HorizontalAlignment="Left">
            <Ellipse.Fill>
                <ImageBrush ImageSource="/Images/icon.png"/>
            </Ellipse.Fill>
        </Ellipse>
        <TextBlock Text="In da restoraunt" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="28" FontWeight="Bold"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
            <Button x:Name="btnDishes" Content="Блюда" Click="btnDishes_Click" Margin="5" Padding="10,5" Visibility="Collapsed"/>
            <Button x:Name="btnOrders" Content="Заказы" Click="btnOrders_Click" Margin="5" Padding="10,5" Visibility="Collapsed"/>
            <Button x:Name="btnLogout" Content="Выйти" Click="btnLogout_Click" Margin="5" Padding="10,5" Visibility="Collapsed"/>
        </StackPanel>

        <Frame Grid.Row="1" Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
